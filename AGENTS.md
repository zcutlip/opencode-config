# Global Agent Instructions

## OpenCode Documentation

When answering questions about OpenCode, check the local documentation
at ~/.opencode-docs/docs/ first before relying on training knowledge.

- Use `docs_manifest.json` to discover available topics
- Read relevant `.md` files from that directory
- The `/docs` command is also available for explicit lookups
